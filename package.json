{
  "name": "rising-intelligence",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty false --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@rising-intelligence/ops-cli": "0.0.0",
    "typescript": "^5.5.4"
  }
}
